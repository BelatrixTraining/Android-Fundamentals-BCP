<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.belatrix.bxtemplate">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>
        <activity android:name=".SimpleListActivity" />
        <activity android:name=".SimpleGridActivity" />
        <activity android:name=".CustomListActivity" />
        <activity android:name=".MovieGridActivity" />
        <activity android:name=".RecyclerActivity" />
        <activity android:name=".RecyclerEventsActivity" />
        <activity android:name=".MovieDetailActivity" />
        <activity android:name=".RecyclerMultipleActivity" />
        <activity android:name=".RecetaActivity">
            <!--<intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>-->
        </activity>
        <activity android:name=".RecetaDetalleActivity" />
        <activity android:name=".DynamicContentActivity"></activity>
    </application>

</manifest>